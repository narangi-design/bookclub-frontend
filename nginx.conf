server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router v7 (client-side routing) — mirrors vercel.json's rewrite.
    location / {
        try_files $uri /index.html;
    }
}
