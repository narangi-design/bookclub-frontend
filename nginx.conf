server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router v7 (client-side routing) — fall back to index.html for unknown paths.
    location / {
        try_files $uri /index.html;
    }
}
